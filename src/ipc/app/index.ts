import { getDiskUsage, checkDependencies, installDependencies, getPlatform, getAppVersion } from "./handlers";

export const app = {
  getDiskUsage,
  checkDependencies,
  installDependencies,
  getPlatform,
  getAppVersion,
};
